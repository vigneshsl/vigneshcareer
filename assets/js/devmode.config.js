/**
 * devmode.config.js — Where the Dev Mode API lives.
 *
 * Public URL only, never a secret: the password hash, session key and GitHub
 * token all stay in the hosting platform's environment variables.
 *
 * Leave empty to talk to the same origin, which is what the local server at
 * http://127.0.0.1:4321 serves. Set it to the deployed service (e.g. a Render
 * web service) to run Dev Mode from the published GitHub Pages site.
 */
export const DEVMODE_API_BASE = '';
